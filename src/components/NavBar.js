import React from "react";
import "./NavBar.css";

const NavBar = () => {
  return (
    <div className="navbar">
      <nav className="home">Home</nav>
      <nav className="client">Client</nav>
    </div>
  );
};

export default NavBar;
